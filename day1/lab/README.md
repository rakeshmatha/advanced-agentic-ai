# Day 1 Lab

All Day 1 hands-on code belongs here. The application implements the selected
customer-service RAG workflow using Python, LangChain, and LangGraph.

## Run

```powershell
python -m day1.lab.ask "Can I return an unopened item?"
```

Add policy files to `documents/`. The local Chroma store is created in `.chroma/`.