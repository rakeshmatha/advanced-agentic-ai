# Foundation

This folder demonstrates the minimum Python-to-LLM path:

1. Load configuration from `.env`.
2. Create an OpenAI client through `mini_agent.client`.
3. Send a prompt and print the response.

The example is intentionally small. Prompt patterns, tokenization, and model
parameters are recorded here before adding retrieval or orchestration.