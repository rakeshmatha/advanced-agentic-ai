# Getting Started

This project is a local document question-answering application built with LangChain.
Add Markdown or plain-text files to this directory. The application splits those files
into chunks, creates OpenAI embeddings, stores them in Chroma, and retrieves relevant
chunks before generating an answer.